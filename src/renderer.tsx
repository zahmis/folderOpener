import React from 'react';
import ReactDOM from 'react-dom';

const App: React.FC = () => <h1 className="test-center">folder opner app</h1>;

ReactDOM.render(<App />, document.getElementById('root'));